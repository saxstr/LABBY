<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#E0E0E0"/> <!-- Background color -->
    <corners android:radius="16dp"/>  <!-- Adjust the radius to change the roundness -->
    <padding
        android:left="15dp"
        android:right="15dp"
        android:top="15dp"
        android:bottom="15dp"/>
</shape>
